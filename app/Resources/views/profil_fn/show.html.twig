{% extends 'base.html.twig' %}

{% block body %}
    <h1>Profil_fn</h1>

    <table>
        <tbody>
            <tr>
                <th>Id_pfn</th>
                <td>{{ profil_FN.idPFN }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('profilfn_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('profilfn_edit', { 'id': profil_FN.idPFN }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
