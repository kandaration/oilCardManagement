{% extends 'base.html.twig' %}

{% block body %}
    <h1>Profilutilisateur</h1>

    <table>
        <tbody>
            <tr>
                <th>Id_puser</th>
                <td>{{ profilUtilisateur.idPUser }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('profiluser_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('profiluser_edit', { 'id': profilUtilisateur.id_PUser }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
